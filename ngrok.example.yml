version: "2"
authtoken: YOUR_NGROK_AUTH_TOKEN

tunnels:
  webapp: # for redirect url if you don't want to use localhost
    proto: http
    addr: webapp:5000

  bot: # for interactions endpoint url
    proto: http
    addr: bot:4000

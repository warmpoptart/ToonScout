services:
  webapp:
    container_name: ts-webapp
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - /app/.next
    ports:
      - "5000:5000"
    env_file: .env